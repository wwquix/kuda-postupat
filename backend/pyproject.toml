[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: requires access to the live BGEU website"]
addopts = "-m 'not live'"

[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]
